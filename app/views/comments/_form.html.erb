<h6>Join the discussion!</h6>

<%= form_for [@article, @comment] do |f| %>
  <p>
    <%= f.label :body, "Add your comment here:" %><br/>
    <%= f.text_area :body %>
  </p>
  <p>
    <%= f.submit 'Submit' %>
  </p>
<% end %>